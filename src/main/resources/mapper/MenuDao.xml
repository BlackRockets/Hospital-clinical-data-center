<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospitaldatacenter.dao.MenuDao">

    <resultMap type="com.hospitaldatacenter.entity.Menu" id="MenuMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="resourceType" column="resource_type" jdbcType="VARCHAR"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="permission" column="permission" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MenuMap">
        select
          id, name, resource_type, url, permission
        from hospital.menu
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MenuMap">
        select
          id, name, resource_type, url, permission
        from hospital.menu
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="MenuMap">
        select *

        from menu m,follow_up_group_member f,role r,role_menu rm,user u,user_role ur
        where f.user_id=u.user_id and f.role_id=r.id and m.id=rm.menu_id and rm.role_id =r.id and
        ur.user_id = u.user_id
       <!-- <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="resourceType != null and resourceType != ''">
                and resource_type = #{resourceType}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="permission != null and permission != ''">
                and permission = #{permission}
            </if>
        </where>-->
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into hospital.menu(name, resource_type, url, permission)
        values (#{name}, #{resourceType}, #{url}, #{permission})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update hospital.menu
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="resourceType != null and resourceType != ''">
                resource_type = #{resourceType},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="permission != null and permission != ''">
                permission = #{permission},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from hospital.menu where id = #{id}
    </delete>

</mapper>